const { Router } = require("express");
// const { authenticate, verifyToken } = require("../../handlers/auth-handlers");

module.exports = Router()
//   .get("/", authenticate)
//   .get("/verify-token", verifyToken);
